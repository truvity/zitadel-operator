{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "zitadel-operator-crds chart values",
  "description": "This chart ships CRDs only and takes no values. The strict empty schema makes any attempt to configure it a loud error instead of a silent no-op.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "global": { "type": "object" }
  }
}
