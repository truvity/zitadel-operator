---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: identityproviders.zitadel.truvity.io
spec:
  group: zitadel.truvity.io
  names:
    kind: IdentityProvider
    listKind: IdentityProviderList
    plural: identityproviders
    singular: identityprovider
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IdentityProvider is the Schema for the identityproviders API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IdentityProviderSpec defines the desired state of IdentityProvider.
            properties:
              autoCreation:
                description: AutoCreation enables automatic user creation on first
                  login.
                type: boolean
              autoUpdate:
                description: AutoUpdate enables automatic user profile update on login.
                type: boolean
              clientIdFromSecret:
                description: ClientIdFromSecret references the Secret containing the
                  client ID.
                properties:
                  key:
                    description: Key is the key within the Secret data.
                    type: string
                  name:
                    description: Name is the name of the Secret.
                    type: string
                  namespace:
                    description: Namespace is the namespace of the Secret.
                    type: string
                required:
                - key
                - name
                - namespace
                type: object
              clientSecretFromSecret:
                description: ClientSecretFromSecret references the Secret containing
                  the client secret.
                properties:
                  key:
                    description: Key is the key within the Secret data.
                    type: string
                  name:
                    description: Name is the name of the Secret.
                    type: string
                  namespace:
                    description: Namespace is the namespace of the Secret.
                    type: string
                required:
                - key
                - name
                - namespace
                type: object
              loginPolicy:
                description: LoginPolicy configures login behavior for this identity
                  provider.
                properties:
                  allowExternalIdp:
                    description: AllowExternalIdp determines whether external identity
                      providers are allowed.
                    type: boolean
                  allowRegister:
                    description: AllowRegister determines whether self-registration
                      is allowed.
                    type: boolean
                  defaultRedirectUri:
                    description: DefaultRedirectUri is the default redirect URI after
                      login.
                    type: string
                  disableLoginWithEmail:
                    description: DisableLoginWithEmail disables login with email.
                    type: boolean
                  disableLoginWithPhone:
                    description: DisableLoginWithPhone disables login with phone.
                    type: boolean
                  hidePasswordReset:
                    description: HidePasswordReset hides the password reset option
                      on the login page.
                    type: boolean
                type: object
              scopes:
                description: Scopes is the list of OAuth scopes to request.
                items:
                  type: string
                type: array
              type:
                description: Type is the identity provider type (google, github, or
                  saml).
                enum:
                - google
                - github
                - saml
                type: string
            required:
            - clientIdFromSecret
            - clientSecretFromSecret
            - type
            type: object
          status:
            description: IdentityProviderStatus defines the observed state of IdentityProvider.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              idpId:
                description: IdpId is the Zitadel identity provider ID.
                type: string
              lastSyncTime:
                description: LastSyncTime is the last time the resource was synced
                  with Zitadel.
                format: date-time
                type: string
              ready:
                description: Ready indicates whether the IdentityProvider is successfully
                  synced.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
