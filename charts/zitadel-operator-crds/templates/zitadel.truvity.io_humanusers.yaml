---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: humanusers.zitadel.truvity.io
spec:
  group: zitadel.truvity.io
  names:
    kind: HumanUser
    listKind: HumanUserList
    plural: humanusers
    singular: humanuser
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.userId
      name: UserID
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: |-
          HumanUser is the Schema for the humanusers API.
          It manages an org-scoped human user (Management API).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HumanUserSpec defines the desired state of HumanUser.
            properties:
              displayName:
                description: DisplayName is the user's display name.
                type: string
              email:
                description: Email is the user's email address.
                type: string
              firstName:
                description: FirstName is the user's first name.
                type: string
              initialPasswordSecretRef:
                description: InitialPasswordSecretRef references a Secret containing
                  the initial password.
                properties:
                  key:
                    description: 'Key is the key within the Secret''s data. Default:
                      "clientSecret"'
                    type: string
                  name:
                    description: Name is the name of the Secret.
                    type: string
                required:
                - name
                type: object
              instance:
                description: |-
                  Instance optionally pins this resource to one operator's instance
                  identity — the operator config's instanceAlias, defaulting to its
                  domain (v0.18 dual-serving). When set to another identity, the CR is
                  ignored entirely so the owning operator can manage it. When empty
                  while the namespace is served by two operators, both fail closed with
                  an AmbiguousInstance condition.
                type: string
              isEmailVerified:
                description: IsEmailVerified marks the email as pre-verified.
                type: boolean
              isPhoneVerified:
                description: IsPhoneVerified marks the phone as pre-verified.
                type: boolean
              lastName:
                description: LastName is the user's last name.
                type: string
              nickName:
                description: NickName is the user's nickname.
                type: string
              organizationId:
                description: |-
                  OrganizationId references a pre-existing Zitadel organization by raw ID.
                  Mutually exclusive with OrganizationRef.
                type: string
              organizationRef:
                description: |-
                  OrganizationRef references an Organization CR managed by this operator.
                  Mutually exclusive with OrganizationId.
                properties:
                  name:
                    description: Name is the name of the referenced resource.
                    type: string
                  namespace:
                    description: |-
                      Namespace is the namespace of the referenced resource.
                      If empty, defaults to the same namespace as the referencing resource.
                    type: string
                required:
                - name
                type: object
              phone:
                description: Phone is the user's phone number.
                type: string
              preferredLanguage:
                description: PreferredLanguage is the user's preferred language (BCP
                  47).
                type: string
              userName:
                description: UserName is the login name of the user.
                type: string
            required:
            - email
            - firstName
            - lastName
            - userName
            type: object
          status:
            description: HumanUserStatus defines the observed state of HumanUser.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncTime:
                description: LastSyncTime is the last time the resource was synced
                  with Zitadel.
                format: date-time
                type: string
              organizationId:
                description: OrganizationId is the resolved organization ID.
                type: string
              ready:
                description: Ready indicates whether the HumanUser is successfully
                  synced.
                type: boolean
              userId:
                description: UserId is the Zitadel user ID.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
