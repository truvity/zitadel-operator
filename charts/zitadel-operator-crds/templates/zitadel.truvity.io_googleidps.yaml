---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: googleidps.zitadel.truvity.io
spec:
  group: zitadel.truvity.io
  names:
    kind: GoogleIdP
    listKind: GoogleIdPList
    plural: googleidps
    singular: googleidp
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.idpID
      name: IdpID
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: |-
          GoogleIdP is the Schema for the googleidps API.
          It manages an instance-scoped Google identity provider (IAM_OWNER, Admin API).
          Uses Admin API AddGoogleProvider/UpdateGoogleProvider.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GoogleIdPSpec defines the desired state of GoogleIdP.
            properties:
              clientId:
                description: ClientId is the Google OAuth2 client ID.
                type: string
              clientSecretRef:
                description: ClientSecretRef references the Secret containing the
                  Google OAuth2 client secret.
                properties:
                  key:
                    description: 'Key is the key within the Secret''s data. Default:
                      "clientSecret"'
                    type: string
                  name:
                    description: Name is the name of the Secret.
                    type: string
                required:
                - name
                type: object
              isAutoCreation:
                description: IsAutoCreation enables automatic user creation (same
                  as IsCreationAllowed in Zitadel API).
                type: boolean
              isAutoUpdate:
                description: IsAutoUpdate enables automatic user profile update on
                  Google login.
                type: boolean
              isCreationAllowed:
                description: IsCreationAllowed allows automatic user creation on first
                  Google login.
                type: boolean
              isLinkingAllowed:
                description: IsLinkingAllowed allows linking existing Zitadel accounts
                  with Google accounts.
                type: boolean
              name:
                description: Name is the display name of the Google identity provider
                  in Zitadel.
                type: string
              scopes:
                description: Scopes are the OAuth2 scopes to request from Google.
                items:
                  type: string
                type: array
            required:
            - clientId
            - clientSecretRef
            - name
            type: object
          status:
            description: GoogleIdPStatus defines the observed state of GoogleIdP.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              idpID:
                description: |-
                  IdpID is the Zitadel identity provider ID for this Google IdP.
                  Exposed so that DefaultLoginPolicy.idps[].idpRef can resolve it.
                type: string
              lastSyncTime:
                description: LastSyncTime is the last time the resource was synced
                  with Zitadel.
                format: date-time
                type: string
              ready:
                description: Ready indicates whether the GoogleIdP is successfully
                  synced.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
