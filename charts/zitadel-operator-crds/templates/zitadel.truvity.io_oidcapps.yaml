---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: oidcapps.zitadel.truvity.io
spec:
  group: zitadel.truvity.io
  names:
    kind: OIDCApp
    listKind: OIDCAppList
    plural: oidcapps
    singular: oidcapp
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.clientId
      name: ClientID
      type: string
    - jsonPath: .status.projectId
      name: Project
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: OIDCApp is the Schema for the oidcapps API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OIDCAppSpec defines the desired state of OIDCApp.
            properties:
              accessTokenRoleAssertion:
                description: |-
                  AccessTokenRoleAssertion includes the user's project role claims in
                  the access token.
                type: boolean
              accessTokenType:
                description: |-
                  AccessTokenType selects the access token format: "bearer" (opaque,
                  default) or "jwt" (self-contained, locally verifiable).
                enum:
                - bearer
                - jwt
                type: string
              authMethod:
                description: AuthMethod is the authentication method.
                enum:
                - basic
                - none
                type: string
              idTokenRoleAssertion:
                description: IdTokenRoleAssertion includes the user's project role
                  claims in the ID token.
                type: boolean
              instance:
                description: |-
                  Instance optionally pins this resource to one operator's instance
                  identity — the operator config's instanceAlias, defaulting to its
                  domain (v0.18 dual-serving). When set to another identity, the CR is
                  ignored entirely so the owning operator can manage it. When empty
                  while the namespace is served by two operators, both fail closed with
                  an AmbiguousInstance condition.
                type: string
              name:
                description: |-
                  Name is the display name of the application in Zitadel.
                  If empty, the Kubernetes resource name is used.
                type: string
              postLogoutRedirectUris:
                description: |-
                  PostLogoutRedirectUris is the list of allowed URIs to return users to
                  after logout (RP-initiated logout). Drift-corrected like RedirectUris.
                items:
                  type: string
                type: array
              projectId:
                description: |-
                  ProjectId references a pre-existing Zitadel project by raw ID.
                  Mutually exclusive with ProjectRef.
                type: string
              projectRef:
                description: |-
                  ProjectRef references a Project CR managed by this operator.
                  Mutually exclusive with ProjectId.
                properties:
                  name:
                    description: Name is the name of the referenced resource.
                    type: string
                  namespace:
                    description: |-
                      Namespace is the namespace of the referenced resource.
                      If empty, defaults to the same namespace as the referencing resource.
                    type: string
                required:
                - name
                type: object
              redirectUris:
                description: |-
                  RedirectUris is the exact, ordered list of allowed OAuth2/OIDC redirect
                  URIs. The operator is the single writer: server-side drift (added or
                  removed entries) is reverted to this list on every reconcile. Zitadel
                  requires https:// URIs unless the app has dev mode enabled.
                items:
                  type: string
                type: array
              secretRef:
                description: SecretRef references the Secret where the client credentials
                  will be stored.
                properties:
                  extraData:
                    additionalProperties:
                      type: string
                    description: ExtraData adds static key-value pairs to the generated
                      secret.
                    type: object
                  keys:
                    description: |-
                      Keys customizes the secret key names for clientId and clientSecret.
                      Defaults: clientId → "client_id", clientSecret → "client_secret"
                    properties:
                      clientId:
                        description: |-
                          ClientId is the key name for the client ID in the generated secret.
                          Default: "client_id"
                        type: string
                      clientSecret:
                        description: |-
                          ClientSecret is the key name for the client secret in the generated secret.
                          Default: "client_secret"
                        type: string
                    type: object
                  name:
                    description: Name is the name of the Secret.
                    type: string
                required:
                - name
                type: object
              type:
                description: Type is the OIDC application type.
                enum:
                - confidential
                - public
                type: string
            required:
            - authMethod
            - redirectUris
            - secretRef
            - type
            type: object
          status:
            description: OIDCAppStatus defines the observed state of OIDCApp.
            properties:
              applicationId:
                description: ApplicationId is the Zitadel application ID.
                type: string
              clientId:
                description: ClientId is the OIDC client ID assigned by Zitadel.
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncTime:
                description: LastSyncTime is the last time the resource was synced
                  with Zitadel.
                format: date-time
                type: string
              organizationId:
                description: OrganizationId is the resolved organization ID (inherited
                  from project).
                type: string
              projectId:
                description: ProjectId is the resolved project ID this app belongs
                  to.
                type: string
              ready:
                description: Ready indicates whether the OIDCApp is successfully synced.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
