---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: emailproviders.zitadel.truvity.io
spec:
  group: zitadel.truvity.io
  names:
    kind: EmailProvider
    listKind: EmailProviderList
    plural: emailproviders
    singular: emailprovider
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.providerId
      name: ProviderID
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: |-
          EmailProvider is the Schema for the emailproviders API.
          It manages an instance-level email provider (IAM_OWNER, Admin API).
          Discriminated type: smtp or http.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              EmailProviderSpec defines the desired state of EmailProvider.
              Discriminated type: exactly one of Smtp or Http must be set.
            properties:
              http:
                description: |-
                  Http configures an HTTP webhook email provider.
                  Mutually exclusive with Smtp.
                properties:
                  endpoint:
                    description: Endpoint is the HTTP(S) URL that receives email notifications.
                    type: string
                required:
                - endpoint
                type: object
              smtp:
                description: |-
                  Smtp configures an SMTP email provider.
                  Mutually exclusive with Http.
                properties:
                  description:
                    description: Description is a human-readable description for this
                      SMTP provider.
                    type: string
                  host:
                    description: Host is the SMTP server hostname.
                    type: string
                  passwordSecretRef:
                    description: PasswordSecretRef references the Secret containing
                      the SMTP password.
                    properties:
                      key:
                        description: 'Key is the key within the Secret''s data. Default:
                          "clientSecret"'
                        type: string
                      name:
                        description: Name is the name of the Secret.
                        type: string
                    required:
                    - name
                    type: object
                  replyToAddress:
                    description: ReplyToAddress is the reply-to email address.
                    type: string
                  senderAddress:
                    description: SenderAddress is the email address used as the sender
                      (From header).
                    type: string
                  senderName:
                    description: SenderName is the display name of the sender.
                    type: string
                  tls:
                    description: Tls enables TLS for the SMTP connection.
                    type: boolean
                  user:
                    description: User is the SMTP authentication username.
                    type: string
                required:
                - host
                - senderAddress
                type: object
            type: object
          status:
            description: EmailProviderStatus defines the observed state of EmailProvider.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncTime:
                description: LastSyncTime is the last time the resource was synced
                  with Zitadel.
                format: date-time
                type: string
              providerId:
                description: ProviderId is the Zitadel email provider ID.
                type: string
              ready:
                description: Ready indicates whether the EmailProvider is successfully
                  synced.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
