---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: passwordcomplexitypolicies.zitadel.truvity.io
spec:
  group: zitadel.truvity.io
  names:
    kind: PasswordComplexityPolicy
    listKind: PasswordComplexityPolicyList
    plural: passwordcomplexitypolicies
    singular: passwordcomplexitypolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.minLength
      name: MinLength
      type: integer
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PasswordComplexityPolicy is the Schema for the passwordcomplexitypolicies
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PasswordComplexityPolicySpec defines the desired state of
              PasswordComplexityPolicy.
            properties:
              hasLowercase:
                description: HasLowercase requires at least one lowercase character.
                type: boolean
              hasNumber:
                description: HasNumber requires at least one numeric character.
                type: boolean
              hasSymbol:
                description: HasSymbol requires at least one special character.
                type: boolean
              hasUppercase:
                description: HasUppercase requires at least one uppercase character.
                type: boolean
              minLength:
                description: MinLength is the minimum password length required.
                type: integer
              organizationId:
                description: |-
                  OrganizationId is the Zitadel organization ID this policy applies to.
                  If empty, applies to the instance level.
                type: string
            required:
            - minLength
            type: object
          status:
            description: PasswordComplexityPolicyStatus defines the observed state
              of PasswordComplexityPolicy.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncTime:
                description: LastSyncTime is the last time the resource was synced
                  with Zitadel.
                format: date-time
                type: string
              ready:
                description: Ready indicates whether the PasswordComplexityPolicy
                  is successfully synced.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
