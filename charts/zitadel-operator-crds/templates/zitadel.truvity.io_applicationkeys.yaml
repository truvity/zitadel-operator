---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: applicationkeys.zitadel.truvity.io
spec:
  group: zitadel.truvity.io
  names:
    kind: ApplicationKey
    listKind: ApplicationKeyList
    plural: applicationkeys
    singular: applicationkey
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.appId
      name: AppId
      type: string
    - jsonPath: .spec.projectRef
      name: Project
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ApplicationKey is the Schema for the applicationkeys API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ApplicationKeySpec defines the desired state of ApplicationKey.
            properties:
              appId:
                description: AppId is the Zitadel application ID to create the key
                  for.
                type: string
              expirationDays:
                description: |-
                  ExpirationDays is the number of days until the key expires.
                  Defaults to 3650 (10 years) if not set.
                type: integer
              keySecretRef:
                description: KeySecretRef references the Secret where the generated
                  key JSON will be stored.
                properties:
                  extraData:
                    additionalProperties:
                      type: string
                    description: ExtraData adds static key-value pairs to the generated
                      secret.
                    type: object
                  keys:
                    description: |-
                      Keys customizes the secret key names for clientId and clientSecret.
                      Defaults: clientId → "client_id", clientSecret → "client_secret"
                    properties:
                      clientId:
                        description: |-
                          ClientId is the key name for the client ID in the generated secret.
                          Default: "client_id"
                        type: string
                      clientSecret:
                        description: |-
                          ClientSecret is the key name for the client secret in the generated secret.
                          Default: "client_secret"
                        type: string
                    type: object
                  name:
                    description: Name is the name of the Secret.
                    type: string
                required:
                - name
                type: object
              projectRef:
                description: ProjectRef is the name of the Zitadel Project CR this
                  key belongs to.
                type: string
            required:
            - appId
            - keySecretRef
            - projectRef
            type: object
          status:
            description: ApplicationKeyStatus defines the observed state of ApplicationKey.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              keyId:
                description: KeyId is the Zitadel application key ID.
                type: string
              lastSyncTime:
                description: LastSyncTime is the last time the resource was synced
                  with Zitadel.
                format: date-time
                type: string
              ready:
                description: Ready indicates whether the ApplicationKey is successfully
                  synced.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
